# Recommended computational submission

Use generate/library.fasta (exactly 50,000 unique eligible strings) and generate/top.fasta (exactly 100 unique library members, ordered by original common quality rank). The ten complete source runs supply 500,000 records and 499,998 identities; runs 11-20 are unused. All ten source methods and necessary inference assets are included. Original run inputs were kept read-only.

The common panel has two related HemoPI2 hemolysis classifiers and one physicochemical heuristic. Its evidence class is MIXED_PREDICTIVE_HEURISTIC; there is no demonstrated antimicrobial-activity endpoint. Q = 0.25 logistic Borda + 0.25 tree Borda + 0.50 heuristic Borda, on the complete fixed comparison population. The scores express an ordinal engineering preference, not MIC, safety or an official score. All candidates have the same panel. The raw median-rank diagnostic overlaps zero of the first 100 Borda leaders, showing substantial endpoint/aggregation sensitivity; the declared default was not changed.

MMR uses lambda 0.90 and unit-cost normalized N-to-C edit similarity on 10,000 strictly top-eligible leaders. It selects 100 plus 100 reserves; exports retain original Q order. Selected first-50 mean Q/diversity are 0.966800/0.813031; all-100 values are 0.960944/0.832243. Quality-only top100 overlap is 79, with mean Q/diversity 0.962126/0.816748. These are computational diagnostics.

Library assembly reserves the chosen 100 and uses proportional allocation across 256 frozen MiniBatchKMeans clusters of standardized length and amino-acid composition, taking fixed-Q leaders within each cluster. Full parameters and fitted representation are retained. The five prescribed 1,024-member library diversity samples give mean 0.819976 and SD 0.001303; reference mean is 0.854845. Mean library length is 19.3385 versus 21.5586 in the pooled candidates. Full distributions, source contributions and diagnostics are in selection/metrics.json and adjacent records.

Identity is normalized N-to-C sequence plus declared linear, canonical L, unmodified free-terminus chemistry. Reverse strings are distinct peptides. Two exact source duplicates were merged; one reverse pair and two palindromes remain recorded in the source pool. The final library has no duplicates, exact reverse pairs or exact forward/reverse matches in the pinned 39,448-member organizer reference or additional 7,795-member comparable DRAMP snapshot. This is bounded public-database coverage. The top100 maximum forward reference Levenshtein.ratio is 0.6923076923, below the official strictly-greater-than-0.8 rejection boundary. Reference novelty uses normalized indel ratio, separate from assembly similarity.

Content validity: the visible official sequence/reference checks pass. Repeatability: fresh Windows and Linux checkouts regenerate all ten pools, verify the full score population, and recompute ranking and assembly into byte-identical final FASTAs twice. No saved source candidate FASTA is copied as inference output. Predictor scores and fitted clustering are frozen assets, with provenance and audit implementations retained.

Full-tier materials are present: abstract, collections, method, complete data/filter/intervention disclosures, source inference code/weights, pinned environment, hashes and reviewer evidence. The public release uses MIT for project-owned components with retained third-party licenses. The default runtime uses the same normalized-indel operation without a GPL dependency; the unchanged organizer validator is separate audit tooling. The exact upstream reference is hash-verified on download and is not mirrored in the public repository. FULL_REQUIREMENTS.md maps the requirements. Public repository verification and Kaggle posting are separate actions; organizer evaluation and antimicrobial efficacy remain unknown.
