# v1.1.0 — full-tier public release

This release preserves the original selected library and ranked top100 byte for byte. It publishes all ten source methods and required weights under the stated MIT/source-specific licenses, adds precise public training-data disclosures, removes the GPL ratio wrapper from default inference, and obtains the unchanged official reference by pinned/hash-verified upstream acquisition instead of mirroring it. The original validator remains available as separate audit tooling.

Fresh Windows and Linux tests passed with unchanged source-pool hashes, all consensus ranks/assembly and both FASTAs. The project follows the full-tier entry-point/seed/license/disclosure requirements. Public repository verification is separate from Kaggle posting. No Kaggle writeup has been posted by this preparation, and biological performance and organizer decisions remain unknown.
